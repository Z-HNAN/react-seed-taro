import { createSelector } from 'reselect'

// export const hiddenSelector = createSelector(
//   [
//     state => state.postInfo.postId
//   ],
//   (postId) => {
//     /* postId 不为空 */
//     return true
//   }
// )
